{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch dedicated server",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "dotnet: build",
      "program": "${workspaceFolder}/bin/Debug/net8.0/HellionServer.dll",
      "args": [
        "-configdir",
        "${workspaceFolder}/bin/Debug/net8.0/"
      ],
      "console": "integratedTerminal"
    }
  ]
}
